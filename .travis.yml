branches:
    only:
        - master

matrix:
    include:
        - language: generic
          install: docker info
          script: bash ci/build-aux-image.sh
    sudo: true
    services:
        - docker
